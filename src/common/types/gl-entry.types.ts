// src/common/types/gl-entry.types.ts

export interface GLEntry {
  documentNumber: string;
  postingDate: string;
  description: string;
  creditAmount: number;
  debitAmount: number;
  accountNumber: string;
}